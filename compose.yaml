services:
  mysql:
    image: 'mysql:latest'
    ports:
      - '3306'
    environment:
      - 'MYSQL_DATABASE=test'
      - 'MYSQL_PASSWORD=test'
      - 'MYSQL_ROOT_PASSWORD=test'
      - 'MYSQL_USER=test'
